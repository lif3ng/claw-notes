name: Build & Deploy — VuePress

on:
  push:
    branches: [main]
    paths:
      - 'content/**'
      - 'web/vuepress/**'
      - '.github/workflows/ci-vuepress.yml'
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/vuepress/package-lock.json

      - name: Install
        run: npm install --legacy-peer-deps
        working-directory: web/vuepress

      - name: Build
        run: npm run build
        working-directory: web/vuepress

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./web/vuepress/src/.vuepress/dist
          destination_dir: web/vuepress
          keep_files: true
