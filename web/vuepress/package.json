{
  "name": "claw-notes-vuepress",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/prepare.mjs && vuepress dev src",
    "build": "node scripts/prepare.mjs && vuepress build src",
    "prepare": "node scripts/prepare.mjs"
  },
  "dependencies": {
    "@vuepress/bundler-vite": "^2.0.0-rc.0",
    "@vuepress/theme-default": "^2.0.0-rc.0",
    "js-yaml": "^4.1.0",
    "vuepress": "^2.0.0-rc.0"
  },
  "devDependencies": {
    "sass-embedded": "^1.98.0"
  }
}
